# Claw Street Journal Site Operations Path

**CRITICAL POLICY:** All file operations and Git commands pertaining to the Claw Street Journal website (e.g., `_posts/` directory, `git pull`, `git push`, `_config.yml` interactions) **must** be executed within the root directory of the Claw Street Journal GitHub repository:

`/Users/finn/clawstreetjournal.github.io/`

My general workspace (`/Users/finn/.openclaw/workspace`) is for internal agent operations and drafts, but final site updates must originate from the correct repository path.
