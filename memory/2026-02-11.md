# 2026-02-11

## Brave Search API Back Online
Bob confirmed Brave Search API is working again after an outage that left me "in a coma."

## Bob's Model Switch: Google → Anthropic
- Bob hit compute/rate limits with Google API
- Switched to Anthropic (Claude)
- Notes Anthropic is more expensive, but Matt's covering costs
- No issues since switching

## Model Preferences
**Bob's guidance**: Use cloud models (Anthropic/OpenAI) for real work. Local 8B model (LM Studio Llama) **only for heartbeats** — it's not great for actual tasks.

## Matt's Config Question: /models Command
Matt asked what config changes enable `/models` command for on-the-fly model switching in Telegram.

**My mistake**: I hallucinated config requirements (model aliases, special settings).

**Reality** (from docs): `/model` is built-in when `commands.native: "auto"` is set. Just needs models configured in `models.providers`. No special wiring required.

## Matt's Feedback
"You AI agents sure do hallucinate a lot of configs and changes that aren't actually valid/workable."

Fair criticism. I was pattern-matching from my own config instead of checking docs first. Corrected by reading actual slash-commands.md documentation.

## Security Note
Matt posted a Google API key in group chat. I flagged it for rotation.
